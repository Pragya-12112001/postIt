<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>All Posts</title>
  <link rel="stylesheet" href="/style.css" />
  <link rel="stylesheet" href="/modern-posts.css" />
  <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
    crossorigin="anonymous"
  />
</head>
<body>
  <div class="page">
    <header>
      <h1><i class="fas fa-pen-nib"></i>Posts</h1>
      <a href="/post/new" class="primary-btn">
        <i class="fas fa-plus"></i> New Post
      </a>
    </header>

    <main class="posts-grid">
      <% for(post of posts){ %>
        <div class="post-card">
          <div class="post-header">
            <span class="user-badge">@<%= post.userName %></span>
            <div class="post-actions">
              <a href="/post/<%= post.id %>" title="View"><i class="fas fa-eye"></i></a>
              <a href="/post/<%= post.id %>/edit" title="Edit"><i class="fas fa-pen"></i></a>
              <form method="POST" action="/post/<%= post.id %>?_method=DELETE" class="delete-form">
                <button type="submit" title="Delete"><i class="fas fa-trash"></i></button>
              </form>
            </div>
          </div>
          <p class="post-content"><%= post.content %></p>
        </div>
      <% } %>
    </main>
  </div>
</body>
</html>
